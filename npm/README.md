# throw-trace

See https://github.com/9wick/throw-trace for full documentation.

## Install

```bash
npm install --save-dev throw-trace
# or
npx throw-trace --help
```

Supports Linux (x64, arm64), macOS (x64, arm64), Windows (x64).
